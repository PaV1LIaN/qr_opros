$diskRootId = (int)($site['UF_DISK_FOLDER_ID'] ?? 0);
$diskFolders = [];
if ($diskRootId > 0 && method_exists($service, 'listDiskFolders')) {
    $diskFolders = $service->listDiskFolders($diskRootId); // [{ID, NAME}, ...]
}
