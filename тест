<?php
require_once($_SERVER['DOCUMENT_ROOT'].'/bitrix/header.php');
global $USER;

require_once($_SERVER['DOCUMENT_ROOT'].'/local/php_interface/lib/pg_master.php'); // getPdo()
require_once($_SERVER['DOCUMENT_ROOT'].'/local/php_interface/lib/lab_disk.php');

// --- helpers ---
function h($s): string { return htmlspecialcharsbx((string)$s); }

function userNameById(int $userId): string {
    if ($userId <= 0) return '';
    $rs = CUser::GetByID($userId);
    if ($u = $rs->Fetch()) {
        $ln = trim((string)$u['LAST_NAME']);
        $fn = trim((string)$u['NAME']);
        return trim($ln . ' ' . $fn);
    }
    return '';
}

function statusBadgeHtml(string $status): string {
    $st = strtoupper(trim($status));

    $map = [
        'NEW'         => ['badge--new',      'Новая'],
        'IN_PROGRESS' => ['badge--progress', 'В работе'],
        'DONE'        => ['badge--done',     'Готово'],
        'REJECTED'    => ['badge--reject',   'Отклонено'],
    ];

    if (isset($map[$st])) {
        [$cls, $label] = $map[$st];
        return '<span class="badge '.$cls.'">'.h($label).'</span>';
    }
    return '<span class="badge badge--other">'.h($status ?: '—').'</span>';
}

// --- approve action (status NEW -> IN_PROGRESS) ---
if (isset($_GET['action'], $_GET['id']) && $_GET['action'] === 'approve' && check_bitrix_sessid()) {
    global $APPLICATION;

    $id = (int)$_GET['id'];

    try {
        $pdo = getPdo();

        // 1) Меняем статус NEW -> IN_PROGRESS
        $stmt = $pdo->prepare("
            UPDATE lab.user_applications
            SET status = 'IN_PROGRESS'
            WHERE id = :id AND status = 'NEW'
        ");
        $stmt->execute([':id' => $id]);

        // Если строка не обновилась (уже не NEW или не найдено) — папку не создаём
        if ($stmt->rowCount() > 0) {
            // 2) Достаём данные заявки (нужны для пути папки)
            $st2 = $pdo->prepare("
                SELECT id, block_id, work_department, disk_folder_id
                FROM lab.user_applications
                WHERE id = :id
                LIMIT 1
            ");
            $st2->execute([':id' => $id]);
            $app = $st2->fetch(PDO::FETCH_ASSOC);

            if ($app) {
                // 3) Создаём папку в Disk (project/dept/app_id) и пишем disk_folder_id в PG
                labEnsureDiskFolderForApp($pdo, $app, (int)$USER->GetID());
            }
        }

        // PRG редирект
        LocalRedirect($APPLICATION->GetCurPageParam('ok=approve', ['action','id','sessid']));

    } catch (Throwable $e) {
        ShowError('Ошибка оформления: ' . htmlspecialcharsbx($e->getMessage()));
    }
}

if (isset($_GET['ok']) && $_GET['ok'] === 'approve') {
    echo '<div class="lab-ok">Статус заявки обновлён.</div>';
}

// --- select list from PG ---
try {
    $pdo = getPdo();

    // Если нужно показывать только свои заявки:
    // $where = "WHERE user_id = :uid";
    // $params = [':uid' => (int)$USER->GetID()];
    // Если нужно показывать все:
    $where = "";
    $params = [];

    $sql = "
        SELECT
            id,
            created_at,
            user_id,
            work_department,
            name_izd,
            id_i,
            date_man,
            status
        FROM lab.user_applications
        {$where}
        ORDER BY created_at DESC
        LIMIT 500
    ";

    $stmt = $pdo->prepare($sql);
    $stmt->execute($params);
    $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);

} catch (Throwable $e) {
    ShowError('Ошибка выборки заявок: ' . h($e->getMessage()));
    require_once($_SERVER['DOCUMENT_ROOT'].'/bitrix/footer.php');
    exit;
}

$rows = is_array($rows) ? $rows : [];
$count = count($rows);

// --- собрать значения для фильтров ---
$statuses = [];
$depts = [];

foreach ($rows as $r) {
    $st = strtoupper(trim((string)($r['status'] ?? '')));
    if ($st !== '') $statuses[$st] = true;

    $dept = trim((string)($r['work_department'] ?? ''));
    if ($dept !== '') $depts[$dept] = true;
}

$statuses = array_keys($statuses);
sort($statuses);

$depts = array_keys($depts);
sort($depts);

// --- супер-красивые стили + гарантированно НЕ обрезает "Действие" ---
// (ширина контейнера остаётся max-width:1152px)
echo '
<style>
  :root{
    --bg1:#f7f8ff;
    --bg2:#f4fbff;
    --text:#0f172a;
    --muted:#64748b;
    --line:#e8ecf4;
    --card:#ffffffcc;
    --card2:#ffffff;
    --shadow: 0 18px 45px rgba(15,23,42,.10);
    --shadow2: 0 10px 25px rgba(15,23,42,.08);
    --ring: 0 0 0 4px rgba(99,102,241,.14);
    --r:16px;
  }

  /* общий фон вокруг */
  body{
    background:
      radial-gradient(1000px 500px at 10% -20%, rgba(99,102,241,.18) 0%, transparent 60%),
      radial-gradient(900px 500px at 110% 0%, rgba(14,165,233,.16) 0%, transparent 55%),
      linear-gradient(180deg, var(--bg1), var(--bg2));
  }

  .lab-wrap{
    max-width:1152px;
    margin:18px auto 26px;
    padding:0 12px;
  }

  .lab-ok{
    max-width:1152px;
    margin:14px auto 0;
    padding:12px 14px;
    border-radius:14px;
    background: linear-gradient(180deg, #ecfdf5, #ffffff);
    border:1px solid #a7f3d0;
    color:#065f46;
    font-weight:800;
    box-shadow: var(--shadow2);
  }

  .lab-card{
    border:1px solid var(--line);
    border-radius: var(--r);
    background: var(--card);
    backdrop-filter: blur(10px);
    box-shadow: var(--shadow);
    overflow:hidden;
  }

  .lab-head{
    padding:16px 16px 14px;
    border-bottom:1px solid var(--line);
    background:
      radial-gradient(900px 240px at 10% 0%, rgba(99,102,241,.14), transparent 55%),
      radial-gradient(700px 220px at 90% 0%, rgba(14,165,233,.12), transparent 55%),
      linear-gradient(180deg, #ffffff, #fbfcff);
  }

  .lab-head-top{
    display:flex;
    align-items:flex-end;
    justify-content:space-between;
    gap:12px;
    flex-wrap:wrap;
    margin-bottom:12px;
  }

  .lab-title{
    margin:0;
    font-size:16px;
    font-weight:900;
    color:var(--text);
    letter-spacing:.01em;
  }

  .lab-sub{
    margin:4px 0 0;
    font-size:12px;
    font-weight:800;
    color:var(--muted);
  }

  .chip{
    display:inline-flex;
    align-items:center;
    gap:8px;
    padding:8px 10px;
    border-radius:999px;
    border:1px solid var(--line);
    background:#fff;
    box-shadow: 0 8px 18px rgba(15,23,42,.06);
    font-size:12px;
    font-weight:900;
    color:var(--text);
    white-space:nowrap;
  }
  .chip b{ color:#111827; }

  .lab-filters{
    display:flex;
    flex-wrap:wrap;
    gap:10px;
    align-items:end;
  }

  .field{
    min-width:180px;
    display:flex;
    flex-direction:column;
    gap:6px;
  }
  .field--wide{ min-width:320px; flex: 1 1 320px; }

  .field label{
    font-size:11px;
    font-weight:900;
    color:var(--muted);
    letter-spacing:.06em;
    text-transform:uppercase;
  }

  .input, .select{
    height:40px;
    border-radius:14px;
    border:1px solid var(--line);
    background: linear-gradient(180deg, #ffffff, #fbfcff);
    padding:0 12px;
    font-size:13px;
    font-weight:700;
    color:var(--text);
    outline:none;
    box-shadow: inset 0 1px 0 rgba(255,255,255,.7);
  }
  .input:focus, .select:focus{
    border-color:#c7d2fe;
    box-shadow: var(--ring);
  }

  .actions{
    display:flex;
    gap:8px;
    margin-left:auto;
    align-items:center;
  }

  .btn-ui{
    height:40px;
    padding:0 12px;
    border-radius:14px;
    border:1px solid var(--line);
    background: #fff;
    font-weight:900;
    font-size:12px;
    color:#334155;
    cursor:pointer;
    transition: transform .08s ease, box-shadow .12s ease, background .12s ease;
    box-shadow: 0 10px 20px rgba(15,23,42,.06);
    white-space:nowrap;
  }
  .btn-ui:hover{
    transform: translateY(-1px);
    box-shadow: 0 14px 28px rgba(15,23,42,.10);
    background: linear-gradient(180deg, #ffffff, #f8fafc);
  }

  /* ВАЖНО: чтобы ничего не обрезалось */
  .lab-table-scroll{
    overflow-x:auto;   /* если где-то всё же тесно — появится скролл внутри, а не обрезка */
    overflow-y:hidden;
    background: var(--card2);
  }

  table.lab-table{
    width:100%;
    border-collapse:separate;
    border-spacing:0;
    table-layout:fixed;  /* фиксируем в рамки 1152 */
    color:var(--text);
    font-size:13px;
  }

  /* колонки в % => всегда влезает в контейнер */
  .c-id{width:6%}
  .c-created{width:12%}
  .c-init{width:14%}
  .c-dept{width:10%}
  .c-name{width:22%}
  .c-num{width:10%}
  .c-date{width:10%}
  .c-status{width:8%}
  .c-act{width:8%}   /* узкая, но тут будут ИКОНКИ (влезает всегда) */

  thead th{
    position:sticky;
    top:0;
    z-index:2;
    background: linear-gradient(180deg, #f7f8ff, #f7fbff);
    color:#334155;
    font-weight:900;
    padding:12px 10px;
    border-bottom:1px solid var(--line);
    white-space:nowrap;
  }

  tbody td{
    padding:12px 10px;
    border-bottom:1px solid #f0f3fa;
    vertical-align:top;
    overflow:hidden;
    text-overflow:ellipsis;
    word-break:break-word;
  }

  tbody tr{
    background:#fff;
  }
  tbody tr:nth-child(even){
    background:#fcfdff;
  }
  tbody tr:hover{
    background: linear-gradient(90deg, rgba(99,102,241,.06), rgba(14,165,233,.05));
  }

  .muted{ color:var(--muted); font-weight:700; }

  .badge{
    display:inline-flex;
    align-items:center;
    gap:6px;
    padding:6px 10px;
    border-radius:999px;
    font-weight:900;
    font-size:12px;
    line-height:1;
    border:1px solid transparent;
    white-space:nowrap;
  }
  .badge::before{
    content:"";
    width:8px;height:8px;border-radius:50%;
    background:currentColor;
    opacity:.9;
  }
  .badge--new{color:#1d4ed8;background:#eff6ff;border-color:#dbeafe}
  .badge--progress{color:#b45309;background:#fffbeb;border-color:#fde68a}
  .badge--done{color:#047857;background:#ecfdf5;border-color:#a7f3d0}
  .badge--reject{color:#b91c1c;background:#fef2f2;border-color:#fecaca}
  .badge--other{color:#475569;background:#f1f5f9;border-color:#e2e8f0}

  /* ДЕЙСТВИЯ: вместо широких кнопок — иконки (гарантированно влезают) */
  .act{
    display:flex;
    justify-content:flex-start;
    align-items:center;
    gap:6px;
    white-space:nowrap;
  }

  .iconbtn{
    width:32px;
    height:32px;
    border-radius:12px;
    border:1px solid var(--line);
    background: linear-gradient(180deg, #ffffff, #f8fafc);
    display:inline-flex;
    align-items:center;
    justify-content:center;
    text-decoration:none;
    box-shadow: 0 10px 20px rgba(15,23,42,.08);
    transition: transform .08s ease, box-shadow .12s ease, background .12s ease;
  }
  .iconbtn:hover{
    transform: translateY(-1px);
    box-shadow: 0 16px 30px rgba(15,23,42,.12);
    background: linear-gradient(180deg, #ffffff, #eef2ff);
  }
  .iconbtn svg{ width:16px; height:16px; color:#334155; }

  .iconbtn--ok:hover{ background: linear-gradient(180deg, #ffffff, #ecfdf5); }
  .iconbtn--ok svg{ color:#065f46; }

  .iconbtn--edit:hover{ background: linear-gradient(180deg, #ffffff, #eff6ff); }
  .iconbtn--edit svg{ color:#1e3a8a; }

  .iconbtn--folder:hover{ background: linear-gradient(180deg, #ffffff, #eef2ff); }
  .iconbtn--folder svg{ color:#1e3a8a; }

  .empty{
    padding:18px 16px;
    color:var(--muted);
    font-weight:800;
    background:#fff;
  }
</style>
';

// --- header + filters + table ---
echo '<div class="lab-wrap">';
echo '  <div class="lab-card">';
echo '    <div class="lab-head">';
echo '      <div class="lab-head-top">';
echo '        <div>';
echo '          <p class="lab-title">Список заявок</p>';
echo '          <p class="lab-sub">Всего: <b>'.(int)$count.'</b> • Показано: <b id="labVisibleCount">'.(int)$count.'</b></p>';
echo '        </div>';
echo '        <div class="chip">Фильтры активны: <b id="labActiveFilters">0</b></div>';
echo '      </div>';

echo '      <div class="lab-filters">';
echo '        <div class="field field--wide">';
echo '          <label for="labSearch">Поиск</label>';
echo '          <input class="input" id="labSearch" type="text" placeholder="Номер / инициатор / цех / деталь / статус…">';
echo '        </div>';

echo '        <div class="field">';
echo '          <label for="labStatus">Статус</label>';
echo '          <select class="select" id="labStatus">';
echo '            <option value="">Все</option>';
foreach ($statuses as $st) {
    echo '        <option value="'.h($st).'">'.h($st).'</option>';
}
echo '          </select>';
echo '        </div>';

echo '        <div class="field">';
echo '          <label for="labDept">Цех/Отдел</label>';
echo '          <select class="select" id="labDept">';
echo '            <option value="">Все</option>';
foreach ($depts as $d) {
    echo '        <option value="'.h($d).'">'.h($d).'</option>';
}
echo '          </select>';
echo '        </div>';

echo '        <div class="actions">';
echo '          <button class="btn-ui" type="button" id="labClear">Сбросить</button>';
echo '        </div>';
echo '      </div>';
echo '    </div>'; // head

if ($count === 0) {
    echo '<div class="empty">Пока нет заявок для отображения.</div>';
    echo '  </div></div>';
    require_once($_SERVER['DOCUMENT_ROOT'].'/bitrix/footer.php');
    exit;
}

$svgEdit = '<svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M12 20h9" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>';
$svgOk   = '<svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M20 6 9 17l-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>';
$svgFold = '<svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M3 7a2 2 0 0 1 2-2h5l2 2h7a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7Z" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/></svg>';

echo '<div class="lab-table-scroll">';
echo '<table class="lab-table" id="labTable">';
echo '<colgroup>
        <col class="c-id">
        <col class="c-created">
        <col class="c-init">
        <col class="c-dept">
        <col class="c-name">
        <col class="c-num">
        <col class="c-date">
        <col class="c-status">
        <col class="c-act">
      </colgroup>';

echo '<thead>
        <tr>
          <th scope="col">Номер</th>
          <th scope="col">Создан</th>
          <th scope="col">Инициатор</th>
          <th scope="col">Цех/Отдел</th>
          <th scope="col">Наименование детали</th>
          <th scope="col">Номер детали</th>
          <th scope="col">Дата изготовл.</th>
          <th scope="col">Статус</th>
          <th scope="col">Действие</th>
        </tr>
      </thead>';

echo '<tbody>';

foreach ($rows as $r) {
    $id = (int)($r['id'] ?? 0);
    $initiator = userNameById((int)($r['user_id'] ?? 0));

    $createdAtRaw = (string)($r['created_at'] ?? '');
    $createdAt = $createdAtRaw ? date('d.m.Y H:i', strtotime($createdAtRaw)) : '—';

    $dateManRaw = (string)($r['date_man'] ?? '');
    $dateMan = $dateManRaw ? date('d.m.Y', strtotime($dateManRaw)) : '—';

    $status = (string)($r['status'] ?? '');
    $st = strtoupper(trim($status));

    $dept = trim((string)($r['work_department'] ?? ''));
    $name = trim((string)($r['name_izd'] ?? ''));
    $num  = trim((string)($r['id_i'] ?? ''));

    // --- actions (иконки, чтобы гарантированно влезало) ---
    $actHtml = '<div class="act">';
    if ($st === 'NEW') {
        $actHtml .= '<a class="iconbtn iconbtn--edit" href="edit.php?id='.$id.'" title="Редактировать">'.$svgEdit.'</a>';
        $actHtml .= '<a class="iconbtn iconbtn--ok" href="index.php?action=approve&id='.$id.'&sessid='.bitrix_sessid().'" title="Оформить">'.$svgOk.'</a>';
    } else {
        $actHtml .= '<a class="iconbtn iconbtn--folder" href="files.php?id='.$id.'" title="Папка">'.$svgFold.'</a>';
    }
    $actHtml .= '</div>';

    // строка для поиска (в data-search)
    $searchStr = mb_strtolower(trim(implode(' ', [
        (string)$id,
        $createdAt,
        $initiator,
        $dept,
        $name,
        $num,
        $dateMan,
        $status
    ])));

    echo '<tr'
        .' data-status="'.h($st).'"'
        .' data-dept="'.h($dept).'"'
        .' data-search="'.h($searchStr).'"'
        .'>';

    echo '  <td><b>'.(int)$id.'</b></td>';
    echo '  <td>'.h($createdAt).'</td>';
    echo '  <td title="'.h($initiator).'">'.h($initiator ?: '—').'</td>';
    echo '  <td title="'.h($dept).'">'.h($dept ?: '—').'</td>';
    echo '  <td title="'.h($name).'">'.h($name ?: '—').'</td>';
    echo '  <td title="'.h($num).'">'.h($num ?: '—').'</td>';
    echo '  <td>'.h($dateMan).'</td>';
    echo '  <td>'.statusBadgeHtml($status).'</td>';
    echo '  <td>'.$actHtml.'</td>';
    echo '</tr>';
}

echo '</tbody></table>';
echo '</div>'; // scroll
echo '  </div>'; // card
echo '</div>';   // wrap

// --- JS: поиск + фильтры ---
echo '
<script>
(function(){
  const searchEl = document.getElementById("labSearch");
  const statusEl = document.getElementById("labStatus");
  const deptEl   = document.getElementById("labDept");
  const clearEl  = document.getElementById("labClear");
  const table    = document.getElementById("labTable");
  const visibleEl= document.getElementById("labVisibleCount");
  const activeEl = document.getElementById("labActiveFilters");

  if (!table) return;

  const rows = Array.from(table.querySelectorAll("tbody tr"));

  function normalize(s){
    return (s || "").toString().trim().toLowerCase();
  }

  function apply(){
    const q = normalize(searchEl ? searchEl.value : "");
    const st = (statusEl ? statusEl.value : "").toString().trim();
    const dp = (deptEl ? deptEl.value : "").toString().trim();

    let visible = 0;

    rows.forEach(tr => {
      const rowSt = tr.getAttribute("data-status") || "";
      const rowDp = tr.getAttribute("data-dept") || "";
      const rowSearch = (tr.getAttribute("data-search") || "").toLowerCase();

      const okQ  = !q  || rowSearch.indexOf(q) !== -1;
      const okSt = !st || rowSt === st;
      const okDp = !dp || rowDp === dp;

      const show = okQ && okSt && okDp;
      tr.style.display = show ? "" : "none";
      if (show) visible++;
    });

    if (visibleEl) visibleEl.textContent = String(visible);

    let active = 0;
    if (q) active++;
    if (st) active++;
    if (dp) active++;
    if (activeEl) activeEl.textContent = String(active);
  }

  function clearAll(){
    if (searchEl) searchEl.value = "";
    if (statusEl) statusEl.value = "";
    if (deptEl) deptEl.value = "";
    apply();
    if (searchEl) searchEl.focus();
  }

  if (searchEl) searchEl.addEventListener("input", apply);
  if (statusEl) statusEl.addEventListener("change", apply);
  if (deptEl) deptEl.addEventListener("change", apply);
  if (clearEl) clearEl.addEventListener("click", clearAll);

  apply();
})();
</script>
';

require_once($_SERVER['DOCUMENT_ROOT'].'/bitrix/footer.php');
?>
