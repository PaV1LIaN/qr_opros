/* Панель теперь fixed — не режется низом таблицы */
.mini-panel{
  position:fixed;        /* было absolute */
  left:0;
  top:0;
  width:300px;
  max-width:calc(100vw - 40px);
  background:#fff;
  border:1px solid #e7e9ee;
  border-radius:14px;
  box-shadow:0 18px 45px rgba(16,24,40,.18);
  padding:10px;
  z-index:9999;
}

/* маленькая страховка: если тема/шаблон ограничивает overflow */
.lab-card, .table-wrap, table.lab-table, tbody, tr, td{
  overflow:visible;
}
